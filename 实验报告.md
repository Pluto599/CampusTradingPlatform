# 校园二手交易平台 - 实验报告

## 一、实验概述

### 1.1 项目背景
本实验基于提供的UML设计图（类图、用例图、时序图、组件图），完整实现了一个校园二手交易平台的命令行应用程序。项目采用Java语言开发。

### 1.2 项目结构

```
校园交易平台/
├── UML/                    # UML设计图
│   ├── class.puml         # 类图
│   ├── usecase.puml       # 用例图
│   ├── sequence_publish_product.puml  # 时序图
│   └── component.puml     # 组件图
├── src/
│   ├── model/             # 实体类
│   │   ├── User.java
│   │   ├── Product.java
│   │   ├── Category.java
│   │   ├── Tag.java
│   │   ├── Favorite.java
│   │   ├── BrowseHistory.java
│   │   ├── Conversation.java
│   │   └── Message.java
│   ├── repository/        # 数据存储层
│   │   └── Database.java
│   ├── service/           # 业务逻辑层
│   │   ├── AuthService.java
│   │   ├── ProductService.java
│   │   ├── SearchService.java
│   │   ├── ChatService.java
│   │   └── UserService.java
│   ├── util/              # 工具类
│   │   ├── PasswordUtil.java
│   │   └── ValidationUtil.java
│   └── CampusMarketApp.java  # 主程序
└── README.md
```

---

## 二、UML图到代码的转换实现

### 2.1 转换策略与方法

#### 2.1.1 类图转换

**1. 实体类转换**
- 将类图中的每个类创建为独立的Java文件
- 类的属性直接映射为Java类的私有成员变量
- 为每个属性生成get/set方法
- 类的方法直接转换为Java方法

**示例：User类的转换**
```
UML类图设计：
User
- id: Long
- username: String
- email: String
+ register()

转换为

Java代码：
public class User {
    private Long id;
    private String username;
    private String email;
    
    public Date getRegisteredAt() { ... }
    public Date setRegisteredAt() { ... }
}
```

**2. 关系转换**
- **关联关系**：使用成员变量表示（如User持有Product的ID）
- **聚合关系**：使用List集合表示一对多关系（如Conversation包含多个Message）
- **依赖关系**：通过方法参数或局部变量实现（如Service依赖Model）

**3. 服务类转换**
- 将类图中的服务接口转换为Service类
- 每个服务类注入所需的依赖（如Database、AuthService等）
- 实现类图中定义的所有业务方法

#### 2.1.2 用例图转换
将用例图中的每个用例转换为Service层的方法：

| 用例      | 实现方法                    | 所在类                |
| --------- | --------------------------- | --------------------- |
| 注册/登录 | `registerUser()`, `login()` | `AuthService.java`    |
| 发布商品  | `createProduct()`           | `ProductService.java` |
| 搜索商品  | `search()`                  | `SearchService.java`  |
| 收藏商品  | `addFavorite()`             | `UserService.java`    |
| 站内沟通  | `sendMessage()`             | `ChatService.java`    |

#### 2.1.3 时序图转换
按照时序图的交互流程实现业务逻辑：

**示例：发布商品时序图转换**
```java
// 在 ProductService.createProduct() 中实现
public Product createProduct(String title, BigDecimal price, ...) {
    // 1. 验证用户身份（对应时序图：Auth验证）
    if (!authService.isLoggedIn()) {
        return null;
    }
    
    // 2. 验证输入（对应时序图：验证数据）
    if (!ValidationUtil.isValidProductTitle(title)) {
        return null;
    }
    
    // 3. 创建商品对象（对应时序图：创建Product）
    Long productId = db.generateProductId();
    Product product = new Product(productId, title, price, ...);
    
    // 4. 存储到数据库（对应时序图：INSERT product）
    db.addProduct(product);
    
    // 5. 返回结果（对应时序图：返回productId）
    return product;
}
```

#### 2.1.4 组件图转换
将组件图的分层结构映射为包结构：

```
组件图分层 → Java包结构
Frontend → CampusMarketApp.java（主程序）
Service层 → src/service/（业务逻辑）
Repository层 → src/repository/（数据存储）
Model层 → src/model/（实体类）
```

### 2.2 代码规模统计

**最终实现的软件源代码规模：**

| 统计项           | 数量                                                                                |
| ---------------- | ----------------------------------------------------------------------------------- |
| **Java文件总数** | 17个                                                                                |
| **代码总行数**   | 约2000行                                                                           |
| **实体类**       | 8个（User, Product, Category, Tag, Favorite, BrowseHistory, Conversation, Message） |
| **服务类**       | 5个（AuthService, ProductService, SearchService, ChatService, UserService）         |
| **工具类**       | 2个（PasswordUtil, ValidationUtil）                                                 |
| **数据存储类**   | 1个（Database）                                                                     |
| **主程序**       | 1个（CampusMarketApp，约600行）                                                     |


**包结构：**
```
src/
├── model/          8个文件
├── service/        5个文件
├── repository/     1个文件
├── util/           2个文件
└── CampusMarketApp.java
```

---

## 三、大模型辅助代码实现

### 3.1 使用的大模型
本项目主要使用 **GitHub Copilot** 和 **Claude/ChatGPT** 辅助开发。

### 3.2 大模型辅助的工作内容

#### 3.2.1 架构设计与代码生成

**1. 初始代码框架生成**
- **提示词**：
  ```
  根据以下UML类图，生成Java实体类User：
  - 属性：id, username, email, phone, passwordHash, creditScore
  - 需要包含构造函数、getter/setter
  - 使用Date类型存储注册时间
  ```
- **大模型输出**：完整的User.java类代码
- **我的调整**：添加了toString()方法，调整了构造函数逻辑

**2. 服务类模板生成**
- **提示词**：
  ```
  创建ProductService类，需要包含以下方法：
  - createProduct()：发布商品
  - updateProduct()：编辑商品
  - deleteProduct()：删除商品
  - 需要注入Database和AuthService依赖
  ```
- **大模型输出**：基本的服务类结构
- **我的调整**：添加了权限验证逻辑、输入验证、错误处理

#### 3.2.2 工具类实现

**3. 密码加密工具**
- **提示词**：
  ```
  实现一个PasswordUtil类，使用SHA-256算法加密密码，
  包含hashPassword()和verifyPassword()方法
  ```
- **大模型输出**：完整的加密实现代码
- **我的调整**：测试了加密正确性，添加了异常处理

**4. 输入验证工具**
- **提示词**：
  ```
  实现ValidationUtil类，包含：
  - 用户名验证（3-20位字母数字下划线）
  - 邮箱格式验证（正则表达式）
  - 手机号验证（11位数字，1开头）
  ```
- **大模型输出**：基本的验证方法
- **我的调整**：改进了正则表达式，添加了空值检查

#### 3.2.3 数据存储层实现

**5. Database单例类**
- **提示词**：
  ```
  实现一个单例模式的Database类，用HashMap存储数据，
  提供CRUD方法，支持User、Product、Message等实体
  ```
- **大模型输出**：基本的单例结构和部分CRUD方法
- **我的调整**：
  - 添加了所有实体的CRUD方法
  - 实现了自动ID生成器
  - 添加了复杂查询方法（如按状态筛选、按用户ID查询等）

#### 3.2.4 业务逻辑实现

**6. 商品发布流程**
- **提示词**：
  ```
  实现ProductService.createProduct()方法，步骤：
  1. 检查用户登录状态
  2. 验证输入（标题、价格、描述）
  3. 创建商品对象
  4. 保存到数据库
  5. 返回结果
  ```
- **大模型输出**：基本的业务流程代码
- **我的调整**：
  - 添加了详细的输入验证
  - 增加了分类验证
  - 完善了错误提示信息

**7. 站内消息功能**
- **提示词**：
  ```
  实现ChatService，包含：
  - 创建或获取会话
  - 发送消息
  - 查看历史消息
  需要验证用户权限
  ```
- **大模型输出**：基本的消息发送逻辑
- **我的调整**：
  - 添加了会话查重逻辑
  - 实现了双向消息关联
  - 增加了消息已读状态管理

#### 3.2.5 用户界面实现

**8. 命令行菜单系统**
- **提示词**：
  ```
  实现一个命令行菜单系统，包含：
  - 游客菜单（注册、登录、浏览、搜索）
  - 用户菜单（发布、收藏、消息、个人中心）
  - 使用Scanner读取用户输入
  ```
- **大模型输出**：基本的菜单结构
- **我的调整**：
  - 改进了提示信息
  - 增加了输入验证和错误处理

**9. 商品列表显示**
- **提示词**：
  ```
  实现displayProductList()方法，格式化显示商品列表，
  包含：ID、标题、价格、分类、卖家、状态
  ```
- **大模型输出**：简单的列表输出
- **我的调整**：
  - 添加了分隔线美化
  - 实现了描述截断（超过50字显示省略号）
  - 增加了序号显示

#### 3.2.6 文档编写

**10. JavaDoc注释**
- **提示词**：
  ```
  为以下方法添加JavaDoc注释：
  public boolean addFavorite(Long productId)
  ```
- **大模型输出**：标准格式的JavaDoc
- **我的调整**：添加了更详细的参数说明和返回值说明

---

## 四、大模型生成内容的调整与优化

### 4.1 常见问题及解决方案

#### 问题1：权限验证不完整
**不符合预期的情况：**
```java
// 大模型生成
public boolean deleteProduct(Long productId) {
    db.deleteProduct(productId);
    return true;
}
```

**问题：** 没有验证用户是否有权限删除商品

**我的修改：**
```java
public boolean deleteProduct(Long productId) {
    if (!authService.isLoggedIn()) {
        System.out.println("请先登录！");
        return false;
    }
    
    Product product = db.getProductById(productId);
    if (product == null) {
        System.out.println("商品不存在！");
        return false;
    }
    
    // 检查权限
    if (!product.getOwnerId().equals(authService.getCurrentUser().getId())) {
        System.out.println("您没有权限删除此商品！");
        return false;
    }
    
    db.deleteProduct(productId);
    System.out.println("商品已删除！");
    return true;
}
```

#### 问题2：用户提示不够友好
**不符合预期的情况：**
```java
// 大模型生成
if (!ValidationUtil.isValidEmail(email)) {
    return false;
}
```

**问题：** 没有告诉用户为什么失败

**我的修改：**
```java
if (!ValidationUtil.isValidEmail(email)) {
    System.out.println("邮箱格式不正确！");
    return false;
}
```

#### 问题3：缺少数据一致性维护
**不符合预期的情况：**
```java
// 大模型生成的消息发送
public void sendMessage(Long conversationId, String content) {
    Message message = new Message(...);
    db.addMessage(message);
}
```

**问题：** 没有更新会话的最后消息时间

**我的修改：**
```java
public boolean sendMessage(Long conversationId, String content) {
    // ...验证逻辑...
    
    Message message = new Message(messageId, conversationId, 
                                 currentUser.getId(), receiverId, content);
    db.addMessage(message);
    
    // 更新会话
    conversation.addMessage(message);
    db.updateConversation(conversation);
    
    System.out.println("消息已发送");
    return true;
}
```

### 4.2 调整策略总结

当大模型生成的内容不符合预期时，我采用以下策略：

#### 1. 逐步改进
- 先接受大模型生成的基本框架
- 逐步添加边界检查、异常处理、用户提示
- 测试后再进行优化

#### 2. 重新提示
如果生成的代码偏离太多，我会改进提示词。

#### 3. 分步骤生成
对于复杂功能，分多个小步骤让大模型生成。

#### 4. 代码审查与测试
- 对大模型生成的每个方法都进行手动审查
- 编写测试用例验证功能正确性
- 发现问题后立即修正

---

## 五、项目展示

### 5.1 代码编译与运行结果

**编译展示：**
```
$ .\compile.bat
========================================
  校园二手交易平台 - 编译脚本
========================================

正在编译...

编译成功！

Press any key to continue . . . 
```

**运行展示：**
```
$ .\run.bat  
========================================
  校园二手交易平台
========================================

╔════════════════════════════╗
║   欢迎使用校园二手交易平台   ║
╚════════════════════════════╝

========== 主菜单 ==========
1. 注册
2. 登录
3. 浏览商品
4. 搜索商品
5. 查看商品详情
0. 退出系统
============================
请选择:
```

**注册界面：**
```
========== 用户注册 ==========
用户名 (3-20位字母数字下划线): user
邮箱: user@example.com
手机号: 13344556677
密码 (至少6位): 123456
确认密码: 123456
注册成功！用户ID: 1
```

**游客菜单：**
```
========== 主菜单 ==========
1. 注册
2. 登录
3. 浏览商品
4. 搜索商品
5. 查看商品详情
0. 退出系统
```

**登录界面：**
```
========== 用户登录 ==========
用户名: user
密码: 123456
登录成功！欢迎，user
```

**已登录用户菜单：**
```
========== 用户中心 ==========
当前用户: user
1. 浏览商品
2. 搜索商品
3. 查看商品详情
4. 发布商品
5. 我的发布
6. 我的收藏
7. 浏览历史
8. 站内消息
9. 个人资料
0. 退出登录
```

**发布商品：**
```
========== 发布商品 ==========
商品标题: 二手课本
价格: 5
描述: 九成新

选择分类:
1. 电子产品
2. 书籍教材
3. 生活用品
4. 运动器材
5. 其他
请选择 (1-5): 2
商品发布成功！商品ID: 1
```

**浏览商品：**
```
========== 商品列表 ==========

可选分类:
0. 全部分类
1. 电子产品
2. 书籍教材
3. 生活用品
4. 运动器材
5. 其他

选择分类 (直接回车查看全部):

----------------------------------------
1. [1] 二手课本
   价格: ¥5 | 分类: 书籍教材
   卖家: user | 状态: PUBLISHED
   描述: 九成新
----------------------------------------
```

**搜索商品：**
```
========== 搜索商品 ==========
输入搜索关键词: 课本

找到 1 个商品:

----------------------------------------
1. [1] 二手课本
   价格: ¥5 | 分类: 书籍教材
   卖家: user | 状态: PUBLISHED
   描述: 九成新
----------------------------------------
```

**查看商品详情：**
```
请输入商品ID: 1

========== 商品详情 ==========
商品ID: 1
标题: 二手课本
价格: ¥5
分类: 书籍教材
状态: PUBLISHED
描述: 九成新
发布时间: Sun Nov 09 16:24:41 CST 2025
卖家: user (ID: 1)
卖家信用分: 100
卖家联系方式: 13344556677
============================

商品管理:
1. 编辑商品
2. 下架商品
3. 删除商品
0. 返回
请选择: 
```

**我的发布：**
```
========== 我的发布 ==========

----------------------------------------
1. [1] 二手课本
   价格: ¥5 | 分类: 书籍教材
   卖家: user | 状态: PUBLISHED
   描述: 九成新
----------------------------------------
```

**浏览历史：**
```
========== 浏览历史 ==========
1. [1] 二手课本 (浏览于: Sun Nov 09 16:25:32 CST 2025)
```

**站内消息：**
```
========== 站内消息 ==========
暂无会话
```

**个人资料：**
```
========== 个人资料 ==========
用户ID: 1
用户名: user
邮箱: user@email.com
手机: 13344556677
信用分: 100
注册时间: Sun Nov 09 16:24:21 CST 2025
==============================

1. 编辑资料
0. 返回
请选择:
```

**退出登录：**
```
再见，user

========== 主菜单 ==========
1. 注册
2. 登录
3. 浏览商品
4. 搜索商品
5. 查看商品详情
0. 退出系统
============================
请选择:
```

**退出系统：**
```
感谢使用，再见！
Press any key to continue . . .
```


---

## 六、Git远程代码管理展示

1. 本地仓库初始化与首次提交
```bash
# 初始化仓库并提交
git init
git add .
git commit -m "初始化：添加项目骨架与说明"
# 添加远程仓库（替换为实际仓库地址）
git remote add origin git@github.com:your-org/campus-market.git
git push -u origin main
```

1. 分支开发与 Pull Request（推荐流程）
```bash
# 从主分支拉最新
git checkout main
git pull origin main

# 新建功能分支，开发完成后提交并推送
git checkout -b feat/add-search
git add .
git commit -m "feat: 添加搜索服务和测试用例"
git push -u origin feat/add-search

# 在远程仓库创建 PR，进行 Code Review，合并时使用 Squash 或 Merge 策略
```

3. 合并、冲突解决要点
- 先在本地 rebase 或 merge 最新主分支，解决冲突并完整测试再推送。
- 合并后删除远程分支：git push origin --delete feat/add-search

4. 标签与发布
```bash
# 打标签并推送到远程（用于版本发布）
git tag -a v1.0.0 -m "首次稳定版本"
git push origin v1.0.0
```

5. 常见辅助文件
- .gitignore：确保忽略编译输出、IDE 配置、敏感信息（如 .env、*.keystore）
示例条目：
```
/target/
/*.class
*.log
.idea/
.vscode/
.env
```

6. 持续集成（CI）提示（以 GitHub Actions 为例）
- 在 .github/workflows/ci.yml 中添加简单构建与测试步骤：checkout -> setup JDK -> mvn test -> publish artifact（或 run gradle）
- CI 在 PR 时自动运行，保障合并质量。

7. 常见命令与输出示例
- 查看分支与提交记录：
```bash
git branch        # 查看本地分支
git branch -r     # 查看远程分支
git log --oneline --graph --all
```
- 推送本地更改到远程：
```bash
git push origin main
# 输出示例：
# Enumerating objects: 10, done.
# Counting objects: 100% (10/10), done.
# Delta compression using up to 8 threads
# Compressing objects: 100% (6/6), done.
# Writing objects: 100% (7/7), 1.23 KiB | 630.00 KiB/s, done.
# Total 7 (delta 1), reused 0 (delta 0)
# To git@github.com:your-org/campus-market.git
#  * [new branch]      main -> main
```

